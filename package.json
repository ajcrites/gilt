{
  "version": "0.0.0",
  "name": "gilt",
  "bin": "lib/index.js",
  "types": "lib",
  "scripts": {
    "prepublish": "rm -rf lib && tsc && chmod +x lib/index.js"
  },
  "dependencies": {
    "blessed": "^0.1.81"
  },
  "devDependencies": {
    "@types/blessed": "^0.1.8",
    "typescript": "^2.6.2"
  }
}
